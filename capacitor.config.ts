import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shifaai.doctor',
  appName: 'Alshifa AI Medical Assistant',
  webDir: 'dist'
};

export default config;
