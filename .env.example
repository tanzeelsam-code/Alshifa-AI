# Environment Variables for Alshifa AI Medical Assistant

# =============================================================================
# SUPABASE CONFIGURATION (Required)
# =============================================================================
# Your Supabase project URL and anonymous key
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Audit token for frontend-to-backend validation (must match Supabase Secret)
VITE_AUDIT_TOKEN=your_secure_audit_token_here

# =============================================================================
# OPTIONAL: CLIENT-SIDE API KEYS
# =============================================================================
# These are only used for TTS/STT features that require client-side streaming
# All AI text generation now goes through Supabase Edge Functions (server-side)

# OpenAI (Optional - for Text-to-Speech and Whisper transcription)
VITE_OPENAI_API_KEY=sk-your-openai-key-here

# =============================================================================
# SECURITY NOTE
# =============================================================================
# The following API keys are stored in SUPABASE SECRETS, not here:
# - GEMINI_API_KEY (for medical AI generation)
# - OPENAI_API_KEY (for server-side processing) 
# - ANTHROPIC_API_KEY (for Claude provider)
# - AUDIT_TOKEN (server-side validation)
#
# Set these in Supabase Dashboard → Settings → Vault → Secrets

# =============================================================================
# ENCRYPTION
# =============================================================================
# Salt for local storage encryption (change for production)
VITE_ENCRYPTION_SALT=change_this_to_random_string_in_production
